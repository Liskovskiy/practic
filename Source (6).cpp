#include<stdio.h>
#include "Header.h"
int main(void)
{
    char dictionary[] = "string cola cup umbrella"; 
    char text[] = "English is a West Germanic language of the";
    recognition(text, dictionary);
    return 0;
}